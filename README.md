# accessibilitiescore

A simple Go project using [Gin](https://gin-gonic.com/en/docs/).

## Getting started

```bash
go mod tidy
go run .
```

The server listens on `http://localhost:8080`.

## Routes

- `GET /` returns a basic JSON message
- `GET /healthz` returns a simple health check response
